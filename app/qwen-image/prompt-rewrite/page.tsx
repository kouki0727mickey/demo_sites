"use client";
import {useState} from 'react';
import {LabShell,CopyBlock} from '../../labs/shared';
import {rewritePrompt} from '../../labs/math';
const presets=[['青いキツネのアイコン','A blue fox app icon'],['夕暮れの街並み','A city street at sunset'],['背景だけ透明に','Remove the background from the subject']];
export default function PromptDemo(){const [mode,setMode]=useState('t2i'),[request,setRequest]=useState(presets[0][1]),[style,setStyle]=useState('glossy 3D illustration'),[composition,setComposition]=useState('centered subject, generous empty space'),[lighting,setLighting]=useState('soft studio light'),[preserve,setPreserve]=useState('the subject identity, shape and colors'),[ratio,setRatio]=useState('1:1'),[transparent,setTransparent]=useState(true),[revealed,setRevealed]=useState(false);const result=rewritePrompt(mode,request,{style,composition,lighting,preserve,ratio,transparent});function reset(){setRevealed(false)}return <LabShell number="03" title="プロンプト書き換え — 意図を、具体的に。" lead="何を作るか、何を変えて何を残すか。短い依頼に必要な情報を足してみましょう。" path="/qwen-image/prompt-rewrite" sources={[["生成用 PE-T2I","https://huggingface.co/Qwen/Qwen-Image-2.1-PE-T2I"],["編集用 PE-I2I","https://huggingface.co/Qwen/Qwen-Image-2.1-PE-I2I"]]}><div className="lab-notice">ローカルの文章組み立てデモです。公式の9Bモデルは実行せず、入力と選択項目を固定ルールで結合します。表示文は公式モデルの出力ではありません。</div><div className="lab-grid"><section className="lab-panel"><h2>1. 依頼の種類を選ぶ</h2><div className="lab-tabs">{[['t2i','新しく生成する'],['i2i','画像を編集する']].map(([v,l])=><button key={v} aria-pressed={mode===v} onClick={()=>{setMode(v);reset()}}>{l}</button>)}</div><div className="lab-tabs">{presets.map(([label,text],i)=><button key={label} onClick={()=>{setRequest(text);setMode(i===2?'i2i':'t2i');reset()}}>{label}</button>)}</div><label className="lab-label">短い依頼<textarea value={request} maxLength={1200} onChange={e=>{setRequest(e.target.value);reset()}}/></label>{mode==='t2i'?<><label className="lab-label">見た目<select value={style} onChange={e=>{setStyle(e.target.value);reset()}}><option value="glossy 3D illustration">光沢のある3Dイラスト</option><option value="natural photographic style">自然な写真風</option><option value="flat vector-like illustration">フラットなイラスト風</option></select></label><label className="lab-label">構図<select value={composition} onChange={e=>{setComposition(e.target.value);reset()}}><option value="centered subject, generous empty space">中央に配置・余白を確保</option><option value="wide composition, subject on the left">横長の構図・左に被写体</option><option value="close-up view, clearly visible details">接写・細部を見せる</option></select></label><label className="lab-label">光<select value={lighting} onChange={e=>{setLighting(e.target.value);reset()}}><option value="soft studio light">柔らかなスタジオ光</option><option value="warm sunset light">暖かい夕日の光</option><option value="diffused natural daylight">拡散した自然光</option></select></label><label className="lab-label">希望する縦横比<select value={ratio} onChange={e=>{setRatio(e.target.value);reset()}}>{['1:1','16:9','9:16'].map(v=><option key={v}>{v}</option>)}</select></label></>:<><label className="lab-label">変えずに残すもの<input maxLength={500} value={preserve} onChange={e=>{setPreserve(e.target.value);reset()}}/></label><p className="lab-tip">公式の編集用モデルは画像と文章を受け取ります。このデモは画像を解析せず、参照を &lt;image1&gt; という記号で表します。</p></>}<label className="lab-check"><input type="checkbox" checked={transparent} onChange={e=>{setTransparent(e.target.checked);reset()}}/>透過背景の指定も加える</label><button className="lab-action" disabled={!request.trim()||(mode==='i2i'&&!preserve.trim())} onClick={()=>setRevealed(true)}>2. 具体化した文章を見る →</button></section><section className="lab-panel"><h2>入力と出力を比べる</h2><div className="lab-step-detail"><h3>書き換え前</h3><p>{request||'左に依頼を入力してください。'}</p></div><div className="lab-flow"><div className="lab-node"><b>↓</b><span>{mode==='t2i'?'PE-T2I：生成用':'PE-I2I：編集用'}<small>公式はQwen3.5-VL 9Bを調整したモデル／ここでは文章組み立てを体験</small></span></div></div>{revealed?<div aria-live="polite"><h3>具体化した文章（デモ）</h3><p style={{whiteSpace:'pre-wrap'}}>{result.rewritten_prompt}</p><div className="lab-step-detail"><b>{mode==='t2i'?'追加した情報：見た目・構図・光・縦横比':'追加した情報：対象画像・保持する要素・変更範囲'}</b><p>{mode==='t2i'?'曖昧な部分を埋めると、生成に伝える条件が明確になります。追加した条件が元の意図と合うか確認します。':'変更しない要素も明示すると、編集の範囲を伝えやすくなります。元画像の内容を理解する役割は、公式モデル側が担います。'}</p></div><CopyBlock text={JSON.stringify(result,null,2)} label="出力JSONをコピー"/><p className="lab-tip">{mode==='t2i'?'wh_ratioは希望の縦横比です。':'ratio_followは元画像の縦横比を引き継ぐ指定です。wh_ratioと同時には指定しません。'}</p><a className="lab-action" href={'/qwen-image?prompt='+encodeURIComponent(result.rewritten_prompt)+'&mode='+(mode==='i2i'?'edit':'generate')}>この文章をAlpha Labへ渡す →</a><p className="lab-tip">文章だけを引き継ぎます。サイズ・参照画像は生成画面で指定してください。移動だけでは外部送信しません。</p></div>:<div className="lab-step-detail"><h3>文章を具体化してみましょう</h3><p>左で条件を選び、ボタンを押すと、加わった情報と出力形式を確認できます。</p></div>}</section></div><section className="lab-section"><h2>書き換えモデルと画像生成モデルは別の役割</h2><p>書き換えモデルは、画像を作るための文章を返します。その文章をQwen-Image-2.1へ渡して、初めて画像生成・編集が行われます。文章が詳しくなっても、意図どおりの画像になることを保証するものではありません。</p></section></LabShell>}
