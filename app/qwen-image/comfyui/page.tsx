"use client";
import {useState} from 'react';
import {LabShell,Range} from '../../labs/shared';
const nodes=[['モデルを読み込む','Load models','画像モデル・文章エンコーダ・RGBA用VAEを用意します。モデルを配置してから、ComfyUIの公式テンプレートを開く流れです。'],['文章・参照画像を理解する','Text Encode Qwen Image 2.1','文章と参照画像を、生成に使う条件へ変換します。編集では、残したいものと変えたいものを文章で伝えます。'],['少しずつ画像を作る','Sampling','ノイズから画像の表現を更新していきます。ステップ数は処理の繰り返し回数です。この画面ではGPU計算を行いません。'],['RGBAへ戻して保存する','VAE Decode → Save','圧縮された表現を画像へ戻します。RGBの色に加え、A（アルファ）が透明度を表します。PNGならアルファを保存できます。']];
const sample='/api/qwen-image/result?url='+encodeURIComponent('https://qwen-qwen-image-2-1.hf.space/gradio_api/file=/tmp/gradio/f1048bd6ee9931abcf7ff12f07a93a8a8da8b63f6a1ee9af003a024ff875a972/transparent_anime_bride.png');
export default function ComfyDemo(){const [mode,setMode]=useState('generate'),[step,setStep]=useState(0),[references,setReferences]=useState(1),[steps,setSteps]=useState(40),[show,setShow]=useState(false),[background,setBackground]=useState('checker'),[alpha,setAlpha]=useState<string|null>(null),[error,setError]=useState('');return <LabShell number="01" title="ComfyUI — つないで、透過をつくる。" lead="4つの工程をたどり、最後に実際のPNGの透明度を確認します。" path="/qwen-image/comfyui" sources={[["ComfyUIの発表","https://blog.comfy.org/p/qwen-image-21-in-comfyui-open-weight"],["対応モデル","https://huggingface.co/Comfy-Org/Qwen-Image-2.1"],["公式ワークフロー","https://github.com/Comfy-Org/workflow_templates/blob/main/templates/image_qwen_image_2_1_t2i.json"]]}><div className="lab-notice">学習用の簡略ワークフローです。ComfyUIや生成モデルは実行しません。透過プレビューでは、既存の公式作例を実際に画素解析します。</div><div className="lab-grid"><section className="lab-panel"><h2>1. 作るものを選ぶ</h2><div className="lab-tabs">{[['generate','文章から生成'],['edit','参照画像で編集']].map(([v,l])=><button key={v} aria-pressed={mode===v} onClick={()=>{setMode(v);setStep(0)}}>{l}</button>)}</div>{mode==='edit'?<Range label="参照画像の枚数（説明用）" value={references} min={1} max={10} onChange={setReferences} unit="枚"/>:<p>例：背景が透明なキャラクターのステッカー。</p>}<Range label="生成のステップ数（説明用）" value={steps} min={10} max={50} onChange={setSteps}/><div className="lab-flow">{nodes.map(([title,code],i)=><button className="lab-node" key={title} aria-current={step===i?'step':undefined} onClick={()=>setStep(i)}><b>0{i+1}</b><span>{title}<small>{code}</small></span></button>)}</div><div className="lab-actions"><button className="lab-action" disabled={step===3} onClick={()=>setStep(s=>s+1)}>次の工程へ →</button><button className="lab-secondary" onClick={()=>setStep(0)}>最初に戻す</button></div></section><section className="lab-panel"><h2>2. データの流れを見る</h2><div className="lab-step-detail" aria-live="polite"><h3>{nodes[step][0]}</h3><p>{nodes[step][2]}</p><p><b>{step===0?'入力：モデルファイル → 出力：読み込み済みモデル':step===1?`入力：文章${mode==='edit'?` ＋ 参照画像${references}枚`:''} → 出力：生成条件`:step===2?`入力：生成条件 ＋ ノイズ → ${steps}回更新 → 出力：画像の圧縮表現`:'入力：圧縮表現 → 出力：R・G・B・Aの4チャンネル'}</b></p></div><h3>アルファがあると、何が変わる？</h3><div className="lab-tabs">{[['checker','市松'],['light','白'],['dark','濃い緑']].map(([v,l])=><button key={v} aria-pressed={background===v} onClick={()=>setBackground(v)}>{l}</button>)}</div><div className={'lab-preview '+background}>{show?<img src={sample} alt="公式の透過作例。今回の生成結果ではありません。" onLoad={e=>{try{const img=e.currentTarget,c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;const ctx=c.getContext('2d')!;ctx.drawImage(img,0,0);const data=ctx.getImageData(0,0,c.width,c.height).data;let n=0;for(let i=3;i<data.length;i+=4)if(data[i]<255)n++;setAlpha((n/(c.width*c.height)*100).toFixed(1)+'%');}catch{setError('画素を読み取れませんでした。')}}} onError={()=>setError('公式作例を読み込めませんでした。公式ページでご確認ください。')}/>:<button className="lab-action" onClick={()=>{setShow(true);setError('')}}>公式の透過作例を読み込む</button>}</div>{error&&<p role="alert">{error}</p>}{alpha&&<p role="status"><b>半透明・透明な画素：{alpha}</b>（α &lt; 255）</p>}<p className="lab-tip">既存の公式作例です。設定を変えても画像は生成し直しません。背景色は表示だけに使い、画像へ合成しません。</p></section></div><section className="lab-section"><h2>実際に動かすには</h2><p>最新版ComfyUIでモデルを配置し、TemplatesからQwen-Image-2.1を選びます。透過画像は、背景除去とは別に生成モデルがアルファを出力する仕組みです。</p><div className="lab-actions"><a className="lab-action" href="https://github.com/Comfy-Org/workflow_templates/blob/main/templates/image_qwen_image_2_1_t2i.json" target="_blank" rel="noopener noreferrer">公式テンプレートを開く ↗</a><a className="lab-secondary" href="/qwen-image">Alpha Labで画像を生成する →</a></div></section></LabShell>}
