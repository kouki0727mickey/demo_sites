"use client";
import {useState} from 'react';
import {LabShell,Range,Metric,CopyBlock} from '../../labs/shared';
import {cacheWork} from '../../labs/math';
export default function InferenceDemo(){const [steps,setSteps]=useState(40),[prefix,setPrefix]=useState(60),[cache,setCache]=useState(true),[precision,setPrecision]=useState('bf16'),[gpus,setGpus]=useState(2),[parallel,setParallel]=useState('tp'),[engine,setEngine]=useState('vllm');const work=cacheWork(steps,prefix,cache);return <LabShell number="02" title="推論の高速化 — 同じ計算を減らす。" lead="キャッシュ・FP8・複数GPU。それぞれが何を変えるのか、設定を動かして比較します。" path="/qwen-image/inference" sources={[["vLLM-Omniレシピ","https://recipes.vllm.ai/Qwen/Qwen-Image-2.1"],["SGLang Cookbook","https://docs.sglang.io/cookbook/diffusion/Qwen-Image/Qwen-Image-2.1"],["Qwen公式の対応案内","https://github.com/QwenLM/Qwen-Image-2.1"]]}><div className="lab-notice">計算の仕組みを説明するシミュレーションです。表示する計算量は仮定に基づく単位で、実測速度・GPUメモリ使用量ではありません。</div><div className="lab-grid"><section className="lab-panel"><h2>1. 再利用できる部分を変える</h2><Range label="画像を更新する回数" value={steps} min={10} max={50} onChange={setSteps} unit="回"/><Range label="1回の計算に占める固定部分（仮定）" value={prefix} min={10} max={90} onChange={setPrefix} unit="%"/><label className="lab-check"><input type="checkbox" checked={cache} onChange={e=>setCache(e.target.checked)}/>prefix KV cacheを使う</label><p>文章や参照画像など、生成中に変わらない条件の計算結果を保存し、次のステップで再利用します。</p><p className="lab-tip">画像側の計算は毎回必要です。この例では1回の全体計算を「1単位」と仮定しています。別リクエスト間の再利用は計算に含めません。</p><h3>2. 保存する数値の精度を選ぶ</h3><div className="lab-tabs">{['bf16','fp8'].map(v=><button key={v} aria-pressed={precision===v} onClick={()=>setPrecision(v)}>{v.toUpperCase()}</button>)}</div><p>同じ個数の数値なら、16ビットから8ビットへの変更で保存量は半分。実際には変換しない層や追加データがあるため、モデル全体や速度が半分になるとは限りません。</p></section><section className="lab-panel"><h2>再計算する量を比べる</h2><div className="lab-legend"><span>濃色：固定部分</span><span>淡色：画像側の更新</span></div><p>再利用なし：{steps.toFixed(1)}単位</p><div className="lab-bar"><i style={{width:prefix+'%'}}/><em style={{width:100-prefix+'%'}}/></div><p>選択中の設定：{work.work.toFixed(1)}単位</p><div className="lab-bar"><i style={{width:(cache?prefix/steps:prefix)+'%'}}/><em style={{width:100-prefix+'%'}}/></div><div className="lab-metrics"><Metric label="固定部分の計算" value={work.prefixRuns+'回'}/><Metric label="仮定上の計算削減" value={work.saved.toFixed(1)+'%'}/><Metric label="100万個の数値の保存量" value={precision==='bf16'?'2 MB':'1 MB'} note="重みのみ・十進単位の理論値"/></div><p className="lab-tip">計算式：{cache?`${(prefix/100).toFixed(2)} ＋ ${steps} × ${(1-prefix/100).toFixed(2)}`:`${steps} × 1`}。転送・キャッシュ読み取り・初期化などのコストは省略しています。</p><h3>3. 複数GPUへの分け方</h3><div className="lab-tabs">{[['tp','TP：行列計算を分担'],['ulysses','Ulysses：系列処理を分担']].map(([v,l])=><button key={v} aria-pressed={parallel===v} onClick={()=>setParallel(v)}>{l}</button>)}</div><Range label="GPU数（説明用）" value={gpus} min={1} max={4} onChange={setGpus}/><div className="lab-gpus">{Array.from({length:gpus},(_,i)=><div key={i}><b>GPU {i+1}</b>{parallel==='tp'?'行列の一部分':'系列の一部分'}<br/>{i+1} / {gpus}</div>)}</div><p>{parallel==='tp'?'TPは大きな行列計算を複数GPUで分担します。':'Ulyssesは系列を分割し、GPU間で情報を交換してattentionを計算します。'} 通信が必要なので、GPU数に比例して速くなるわけではありません。</p></section></div><section className="lab-section"><h2>実装を選ぶ</h2><div className="lab-tabs">{[['vllm','vLLM-Omni'],['sglang','SGLang']].map(([v,l])=><button key={v} aria-pressed={engine===v} onClick={()=>setEngine(v)}>{l}</button>)}</div>{engine==='vllm'?<><p>確認時のレシピはPR #7759の導入を指定しています。通常のリリース版だけで動くとは限りません。FP8と並列化にも組み合わせの制約があります。</p><CopyBlock text={'vllm serve Qwen/Qwen-Image-2.1 --omni --port 8091'} label="起動例をコピー"/><p className="lab-tip">導入済み環境向けの基本例です。上の説明用設定を反映したコマンドではありません。</p></>:<><p>Qwenの公式案内では、SGLangの生成・編集・RGBA出力への対応を紹介しています。GPUごとの導入手順はCookbookを参照してください。</p><CopyBlock text={'sglang generate \\\n  --model-path Qwen/Qwen-Image-2.1 \\\n  --prompt "A ceramic teapot on a wooden table" \\\n  --height 1024 --width 1024 \\\n  --num-inference-steps 40 --guidance-scale 1 \\\n  --seed 42 --save-output'} label="公式の基本形をコピー"/></>}<p>FP8はメモリ削減の選択肢です。画質や速度への影響は、GPU・対象の層・実装条件で確認する必要があります。</p></section></LabShell>}
