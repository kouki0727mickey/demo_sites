"use client";
import {useEffect,useRef,useState} from 'react';
import {ArrowLeft,ArrowUpRight,Download,ImagePlus,Layers,LoaderCircle,Sparkles,X,Copy} from 'lucide-react';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
import '../jev/jev.css';
import './qwen.css';
import {generateGpu,gpuRequest} from './gpu';
const official='https://huggingface.co/spaces/Qwen/Qwen-Image-2.1';
const sample='https://qwen-qwen-image-2-1.hf.space/gradio_api/file=/tmp/gradio/f1048bd6ee9931abcf7ff12f07a93a8a8da8b63f6a1ee9af003a024ff875a972/transparent_anime_bride.png';
const presets=[{name:'ドラゴンのステッカー',text:'A friendly little jade-green dragon sticker, rounded shapes, tiny wings, clean silhouette, soft studio lighting, no text.'},{name:'青いキツネのアイコン',text:'A glossy blue fox app icon, charming expression, sculpted glass-like finish, centered composition, no text.'},{name:'商品を切り抜く',text:'Extract the main product from the reference image, preserve its shape and details, remove the background completely.'}];
type RefImage={file:File;url:string};
export default function QwenLab(){const [mode,setMode]=useState('generate'),[prompt,setPrompt]=useState(presets[0].text),[transparent,setTransparent]=useState(true),[enhance,setEnhance]=useState(false),[size,setSize]=useState('1024'),[seed,setSeed]=useState(42),[refs,setRefs]=useState<RefImage[]>([]),[busy,setBusy]=useState(false),[status,setStatus]=useState(''),[error,setError]=useState(''),[result,setResult]=useState(''),[resultKind,setResultKind]=useState(''),[background,setBackground]=useState('checker'),[alpha,setAlpha]=useState<{width:number;height:number;transparent:number}|null>(null),[rewrite,setRewrite]=useState('');const [backend,setBackend]=useState('official'),[gpuBase,setGpuBase]=useState(''),[gpuToken,setGpuToken]=useState(''),[checking,setChecking]=useState(false);const lock=useRef(false),controller=useRef<AbortController|null>(null),refsCurrent=useRef<RefImage[]>([]);const fullPrompt=transparent?`This is an RGBA image with transparency. ${prompt.trim()} The image has alpha channel and the background is transparent.`:prompt.trim();
useEffect(()=>{const params=new URLSearchParams(window.location.search);const incoming=params.get('prompt');if(incoming){setPrompt(incoming.slice(0,4500));setTransparent(false);if(params.get('mode')==='edit')setMode('edit');}},[]);
useEffect(()=>()=>{if(result.startsWith('blob:'))URL.revokeObjectURL(result)},[result]);
useEffect(()=>{refsCurrent.current=refs},[refs]);useEffect(()=>()=>{controller.current?.abort();refsCurrent.current.forEach(r=>URL.revokeObjectURL(r.url))},[]);
function add(files:FileList|null){if(!files)return;const incoming=Array.from(files);if(refs.length+incoming.length>10||incoming.some(f=>!['image/png','image/jpeg','image/webp'].includes(f.type)||f.size>4*1024*1024)||[...refs.map(r=>r.file),...incoming].reduce((n,f)=>n+f.size,0)>20*1024*1024){setError('画像は最大10枚、PNG/JPEG/WebPで1枚4MB・合計20MBまでです。');return;}setError('');setRefs(prev=>[...prev,...incoming.map(file=>({file,url:URL.createObjectURL(file)}))]);}
async function checkGpu(){setChecking(true);setError('');setStatus('専用GPUへの接続を確認中…');try{const data:any=await(await gpuRequest(gpuBase,gpuToken,'/health',{signal:AbortSignal.timeout(15000)})).json();if(!data.ready||data.model!=='Qwen/Qwen-Image-2.1')throw Error('Qwen Image 2.1の読み込みが完了していません。');setStatus(data.busy?'接続できました。GPUは現在生成中です。':'接続できました。専用GPUで生成できます。')}catch(e){setStatus('');setError(e instanceof TypeError?'接続できません。URL・HTTPS・CORS設定とGPUの起動状態を確認してください。':e instanceof Error?e.message:'接続できませんでした。')}finally{setChecking(false)}}
async function generate(){if(lock.current)return;lock.current=true;setBusy(true);setError('');setStatus(backend==='gpu'?'専用GPUへ接続中…':'公式デモへ接続中…');setRewrite('');const abort=new AbortController();controller.current=abort;const timer=setTimeout(()=>abort.abort(),backend==='gpu'?1200000:240000);try{if(backend==='gpu'){const url=await generateGpu(gpuBase,gpuToken,{prompt:fullPrompt,size,seed,files:mode==='edit'?refs.map(r=>r.file):[]},abort.signal,setStatus);setResult(url);setResultKind('専用GPUの生成結果 · seed '+seed);setAlpha(null);setStatus('生成が完了しました');return;}let paths:string[]=[];if(mode==='edit'){setStatus('参照画像を公式デモに送信中…');const form=new FormData();refs.forEach(r=>form.append('files',r.file));const upload=await fetch('/api/qwen-image/upload',{method:'POST',body:form,signal:abort.signal});const data:any=await upload.json();if(!upload.ok)throw Error(data.error);paths=data.paths;}
const start=await fetch('/api/qwen-image',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt:fullPrompt,size,seed,enhance,references:paths}),signal:abort.signal});const started:any=await start.json();if(!start.ok)throw Error(started.error);setStatus('待機・生成中… 混雑状況によって時間がかかります');const response=await fetch('/api/qwen-image?event='+encodeURIComponent(started.event),{signal:abort.signal});if(!response.ok||!response.body)throw Error('生成結果を受信できませんでした。');const reader=response.body.getReader(),decoder=new TextDecoder();let buffer='',done=false;try{while(!done){const chunk=await reader.read();buffer+=decoder.decode(chunk.value||new Uint8Array(),{stream:!chunk.done}).replaceAll('\r\n','\n');let boundary;while((boundary=buffer.indexOf('\n\n'))>=0){const event=buffer.slice(0,boundary);buffer=buffer.slice(boundary+2);const type=event.split('\n').find(l=>l.startsWith('event:'))?.slice(6).trim();const payload=event.split('\n').filter(l=>l.startsWith('data:')).map(l=>l.slice(5).trim()).join('\n');if(type==='error')throw Error('公式デモで生成エラーが発生しました。時間をおいて再実行するか、公式ページをご確認ください。');if(type==='complete'){const data=JSON.parse(payload);const url=data?.[0]?.url;if(typeof url!=='string'||!url.startsWith('https://qwen-qwen-image-2-1.hf.space/gradio_api/file='))throw Error('生成画像のURLを確認できませんでした。');setResult('/api/qwen-image/result?url='+encodeURIComponent(url));setResultKind('今回の生成結果 · seed '+data[1]);setAlpha(null);setRewrite(typeof data[2]==='string'?data[2]:'');setStatus('生成が完了しました');done=true;break;}}if(chunk.done&&!done)throw Error('生成結果が届く前に接続が終了しました。');}}finally{await reader.cancel().catch(()=>{});}}catch(e){setError(abort.signal.aborted?'結果の受信を停止しました（停止操作または制限時間）。生成先の処理は続く場合があります。':e instanceof TypeError?'生成先に接続できません。URL・HTTPS・CORS設定と起動状態を確認してください。':e instanceof Error?e.message:'生成できませんでした。');setStatus('');}finally{clearTimeout(timer);lock.current=false;setBusy(false);}}
function inspect(img:HTMLImageElement){try{const canvas=document.createElement('canvas');canvas.width=img.naturalWidth;canvas.height=img.naturalHeight;const ctx=canvas.getContext('2d')!;ctx.drawImage(img,0,0);const data=ctx.getImageData(0,0,canvas.width,canvas.height).data;let transparent=0;for(let i=3;i<data.length;i+=4)if(data[i]<255)transparent++;setAlpha({width:canvas.width,height:canvas.height,transparent:transparent/(canvas.width*canvas.height)*100});}catch{setAlpha(null);}}
return <main className="jev-page qwen-lab"><header className="topbar"><a className="brand" href="/"><span className="brandmark">a</span>DEMO ATLAS</a><a className="jev-back" href="/"><ArrowLeft size={16}/>サイト一覧へ</a></header><section className="jev-heading"><div><div className="eyebrow">QWEN-IMAGE-2.1 / RGBA STUDIO</div><h1>Alpha <span>Lab.</span></h1><p>背景を持たない画像を、つくる。</p></div><div className="jev-heading-note">公式デモ・Colab・専用GPU。<br/>生成する環境を選べます。<a href={official} target="_blank" rel="noopener noreferrer">公式デモを開く<ArrowUpRight size={14}/></a></div></section><section className="q-backend"><label htmlFor="q-backend">生成する環境</label><select id="q-backend" value={backend} disabled={busy||checking} onChange={e=>{setBackend(e.target.value);setError('');setStatus('')}}><option value="official">公式デモ（共有の待ち行列）</option><option value="colab">Colabで試す</option><option value="gpu">専用GPU（自分のサーバー）</option></select>{backend==='colab'&&<div><h2>まず1枚、Colabで生成する</h2><p>T4向け省メモリ版：古いランタイムを再起動してから最新版を開き、上から実行してください。まず512pxで生成します。CPUへの退避により遅くなります。通常RAMにも余裕が必要で、無料環境での動作は未保証です。</p><nav><a href="https://colab.research.google.com/github/kouki0727mickey/demo_sites/blob/main/public/notebooks/qwen-image-2.1.ipynb" target="_blank" rel="noopener noreferrer">Colabで開く ↗</a><a href="/notebooks/qwen-image-2.1.ipynb" download>ノートブックを保存</a></nav><p>Colabで生成できたら、専用GPUを用意して右のプレビューへ接続できます。Colabを公開APIとして常設する構成ではありません。</p></div>}{backend==='gpu'&&<div><div className="q-gpu-fields"><label>GPUサーバーのHTTPS URL<input type="url" placeholder="https://your-gpu.example.com" value={gpuBase} disabled={busy||checking} onChange={e=>{setGpuBase(e.target.value);setStatus('')}}/></label><label>接続キー<input type="password" autoComplete="off" value={gpuToken} disabled={busy||checking} onChange={e=>{setGpuToken(e.target.value);setStatus('')}}/></label></div><button className="q-stop" disabled={busy||checking||!gpuBase||!gpuToken} onClick={checkGpu}>{checking?'接続を確認中…':'接続を確認'}</button><p>文章・参照画像を指定したGPUへ直接送信します。接続キーはこの画面のメモリだけに保持し、再読み込みで消えます。40ステップで生成します。</p><a href="https://github.com/kouki0727mickey/demo_sites/tree/main/examples/qwen-gpu" target="_blank" rel="noopener noreferrer">専用GPUの起動手順・サーバーコード ↗</a></div>}</section><div className="q-layout"><section className="q-input"><div className="jev-section-title"><span>01 / CREATE</span><span>QWEN IMAGE 2.1</span></div><Tabs value={mode} onValueChange={v=>{setMode(v);setError('')}}><TabsList><TabsTrigger disabled={busy} value="generate">テキストから生成</TabsTrigger><TabsTrigger disabled={busy} value="edit">参照画像で編集</TabsTrigger></TabsList></Tabs><div className="q-presets">{presets.map((p,i)=><button disabled={busy} key={p.name} onClick={()=>{setPrompt(p.text);if(i===2)setMode('edit')}}>{p.name}</button>)}</div><label className="q-label" htmlFor="q-prompt">作りたい画像・編集内容</label><textarea id="q-prompt" maxLength={4500} disabled={busy} value={prompt} onChange={e=>setPrompt(e.target.value)}/>{mode==='edit'&&<div className="q-references"><label className="q-upload"><ImagePlus size={19}/>参照画像を追加 ({refs.length}/10)<input type="file" multiple accept="image/png,image/jpeg,image/webp" disabled={busy} onChange={e=>{add(e.target.files);e.target.value=''}}/></label><div className="q-thumbs">{refs.map((r,i)=><div key={r.url}><img src={r.url} alt={'参照画像 '+(i+1)}/><button disabled={busy} aria-label={'参照画像 '+(i+1)+'を削除'} onClick={()=>{URL.revokeObjectURL(r.url);setRefs(old=>old.filter((_,n)=>n!==i))}}><X size={13}/></button></div>)}</div><p>最大10枚。1枚4MB・合計20MBまで。</p></div>}<label className="q-check"><input type="checkbox" checked={transparent} disabled={busy} onChange={e=>setTransparent(e.target.checked)}/><span>透過背景（RGBA）をリクエスト</span></label><p className="q-hint">透過を指定する文章を自動で追加します。透過の有無は生成後の画素から確認します。</p><div className="q-options"><label>サイズ<select disabled={busy} value={size} onChange={e=>setSize(e.target.value)}><option value="1024">1024 × 1024</option><option value="2048">2048 × 2048</option></select></label><label>シード<input type="number" min="0" max="2147483647" disabled={busy} value={seed} onChange={e=>setSeed(Number(e.target.value))}/></label></div>{backend==='official'&&<label className="q-check"><input type="checkbox" checked={enhance} disabled={busy} onChange={e=>setEnhance(e.target.checked)}/>公式デモのプロンプト補強を使う</label>}<details className="q-prompt-detail"><summary>送信するプロンプト</summary><p>{fullPrompt}</p><button onClick={async()=>{try{await navigator.clipboard.writeText(fullPrompt);setStatus('プロンプトをコピーしました')}catch{setError('コピーできませんでした。文章を選択してコピーしてください。')}}}><Copy size={13}/>コピー</button></details><button className="q-generate" onClick={generate} disabled={busy||checking||backend==='colab'||(backend==='gpu'&&(!gpuBase||!gpuToken))||!prompt.trim()||!Number.isInteger(seed)||seed<0||seed>2147483647||(mode==='edit'&&!refs.length)}>{busy?<><LoaderCircle className="animate-spin" size={18}/>生成を待っています</>:<><Sparkles size={18}/>{backend==='colab'?'Colabのノートブックで実行':mode==='edit'?'画像を編集する':'画像を生成する'}</>}</button>{busy&&<button className="q-stop" onClick={()=>controller.current?.abort()}>受信を停止</button>}{backend==='official'&&<p className="q-hint">実行時に文章・参照画像を公式Hugging Faceデモへ送信します。公式デモの実装では入力と出力を保存します。生成設定・待ち時間・利用制限は公式デモに依存します。</p>}{status&&<p role="status" className="q-status">{status}</p>}{error&&<p role="alert" className="notice error">{error} {backend==='official'&&<a href={official} target="_blank" rel="noopener noreferrer">公式デモ ↗</a>}</p>}</section><section className="q-output"><div className="jev-section-title"><span>02 / ALPHA PREVIEW</span><Layers size={17}/></div><div className="q-backgrounds" aria-label="プレビュー背景">{[['checker','市松模様'],['light','白'],['dark','黒'],['violet','紫']].map(([value,label])=><button key={value} aria-pressed={background===value} onClick={()=>setBackground(value)}>{label}</button>)}</div><div className={'q-canvas bg-'+background}>{result?<img src={result} alt={resultKind} onLoad={e=>inspect(e.currentTarget)} onError={()=>{setError('画像を読み込めませんでした。再度生成してください。');setAlpha(null)}}/>:<div className="q-empty"><Layers size={42} strokeWidth={1}/><h2>透明を、見てみよう。</h2><p>背景を切り替えて、画像の輪郭と<br/>透ける部分を確認できます。</p><button disabled={busy} onClick={()=>{setResult('/api/qwen-image/result?url='+encodeURIComponent(sample));setResultKind('公式の公開作例 · 今回の生成ではありません');setAlpha(null);setError('')}}>公式の透過作例を見る<ArrowUpRight size={15}/></button></div>}</div>{result&&<><div className="q-result-meta"><span>{resultKind}</span><a href={result} download="qwen-image.png"><Download size={15}/>画像を保存</a></div><div className="q-alpha">{alpha?<><strong>{alpha.transparent>0?'透過画素を検出':'透過画素なし'}</strong><span>{alpha.width} × {alpha.height} px · α &lt; 255：{alpha.transparent.toFixed(1)}%</span></>:<span>画像の透過情報を確認中</span>}</div><p className="q-hint">背景色はプレビュー専用です。保存する画像には合成しません。</p><button className="q-stop" disabled={busy} onClick={()=>{setResult('');setAlpha(null);setRewrite('')}}>プレビューをクリア</button></>}{rewrite&&<details className="q-prompt-detail"><summary>公式デモが補強したプロンプト</summary><p>{rewrite}</p></details>}</section></div><section className="q-about"><h2>Qwen-Image-2.1でできること</h2><div><p><b>ネイティブ透過生成</b><br/>背景を後から消すだけでなく、RGBA画像を生成できるモデルです。</p><p><b>最大10枚の参照画像</b><br/>複数の画像を使った編集に対応。公式デモまたは専用GPUで編集できます。</p><p><b>公開ウェイトと公式デモ</b><br/>自前で動かす場合はGPU環境が必要。Colabで試すか、専用GPUをこの画面に接続できます。</p></div><p>公式公開日：2026年9月20日。公開ウェイトはQwen Research Licenseです。利用条件は公式ライセンスを確認してください。公式デモの生成環境は公開ウェイトのローカル実行と同一とは限りません。</p><nav><a href="https://github.com/QwenLM/Qwen-Image-2.1" target="_blank" rel="noopener noreferrer">公式リポジトリ ↗</a><a href="https://huggingface.co/Qwen/Qwen-Image-2.1" target="_blank" rel="noopener noreferrer">モデルカード ↗</a><a href="https://github.com/QwenLM/Qwen-Image-2.1/blob/main/LICENSE" target="_blank" rel="noopener noreferrer">ライセンス ↗</a></nav></section><footer><span>DEMO ATLAS / ALPHA LAB</span><span>QWEN-IMAGE-2.1</span></footer></main>}
