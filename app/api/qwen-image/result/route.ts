import {HOST,noStore} from '@/app/qwen-image/api';
export async function GET(request:Request){const url=new URL(request.url).searchParams.get('url');try{if(!url)throw Error();const parsed=new URL(url);if(parsed.origin!==HOST||parsed.username||parsed.password||!/^\/gradio_api\/file=\/tmp\/gradio\/[a-f0-9]+\/[^/]+$/.test(decodeURIComponent(parsed.pathname)))throw Error();const r=await fetch(parsed.toString(),{redirect:'manual',signal:AbortSignal.timeout(30000)});const type=r.headers.get('content-type')?.split(';')[0]||'';if(!r.ok||!['image/png','image/webp','image/jpeg'].includes(type))throw Error();return new Response(r.body,{headers:{...noStore,'Content-Type':type,'X-Content-Type-Options':'nosniff'}});}catch{return new Response('画像を取得できませんでした。',{status:400,headers:noStore})}}
