import {HOST,inputSchema,dataFor,sameOrigin,noStore} from '@/app/qwen-image/api';
export async function POST(request:Request){if(!sameOrigin(request))return Response.json({error:'この操作は許可されていません。'},{status:403});try{const raw=await request.text();if(raw.length>16000)return Response.json({error:'入力が長すぎます。'},{status:413});const parsed=inputSchema.safeParse(JSON.parse(raw));if(!parsed.success)return Response.json({error:'プロンプト・画像・設定を確認してください。'},{status:400});const r=await fetch(HOST+'/gradio_api/call/generate_with_enhance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({data:dataFor(parsed.data)}),redirect:'manual',signal:AbortSignal.timeout(20000)});if(!r.ok)return Response.json({error:r.status===503?'公式デモの待ち行列が満杯です。少し時間をおいて再実行してください。':'公式デモが生成依頼を受け付けませんでした。公式ページでも状況を確認できます。'},{status:r.status===503?503:502,headers:noStore});const data:any=await r.json();if(typeof data.event_id!=='string'||!/^[a-f0-9-]+$/.test(data.event_id))throw Error();return Response.json({event:data.event_id},{headers:noStore});}catch{return Response.json({error:'公式デモに接続できませんでした。入力内容を確認して再実行してください。'},{status:502,headers:noStore})}}
export async function GET(request:Request){const event=new URL(request.url).searchParams.get('event');if(!event||!/^[a-f0-9-]{10,80}$/.test(event))return new Response('Invalid event',{status:400});try{const r=await fetch(HOST+'/gradio_api/call/generate_with_enhance/'+event,{redirect:'manual',signal:AbortSignal.timeout(240000)});if(!r.ok)return new Response('公式デモの結果を受信できませんでした。',{status:502});return new Response(r.body,{headers:{...noStore,'Content-Type':'text/event-stream'}});}catch{return new Response('公式デモとの接続が終了しました。',{status:502})}}
