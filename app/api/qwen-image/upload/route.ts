import {HOST,sameOrigin,noStore} from '@/app/qwen-image/api';
export async function POST(request:Request){if(!sameOrigin(request))return Response.json({error:'この操作は許可されていません。'},{status:403});try{if(Number(request.headers.get('content-length'))>21*1024*1024)throw Error();const form=await request.formData();const files=form.getAll('files');if(files.length<1||files.length>10)throw Error();let total=0;const upstream=new FormData();for(const file of files){if(typeof file==='string'||!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>4*1024*1024)throw Error();total+=file.size;upstream.append('files',file);}if(total>20*1024*1024)throw Error();const r=await fetch(HOST+'/gradio_api/upload',{method:'POST',body:upstream,redirect:'manual',signal:AbortSignal.timeout(60000)});if(!r.ok)throw Error();const paths:unknown=await r.json();if(!Array.isArray(paths)||paths.length!==files.length||paths.some(p=>typeof p!=='string'||!/^\/tmp\/gradio\/[a-f0-9]+\/[^/\\]+$/.test(p)))throw Error();return Response.json({paths},{headers:noStore});}catch{return Response.json({error:'画像を送信できませんでした。PNG/JPEG/WebP、1枚4MB・合計20MB以内で再実行してください。'},{status:400,headers:noStore})}}
