"use client";
import {useState} from 'react';
import {NewsShell,TextTrial} from '../news/shared';
import {Metric} from '../shared';
const models=[{name:'Pro',api:'mimo-v2.6-pro',total:1020,active:42,experts:384,selected:8,url:'https://huggingface.co/XiaomiMiMo/MiMo-V2.6-Pro-RL'},{name:'Flash',api:'mimo-v2.6-flash',total:309,active:15,experts:256,selected:8,url:'https://huggingface.co/XiaomiMiMo/MiMo-V2.6-Flash-RL'}];
export default function MiMo(){const [index,setIndex]=useState(0),[bits,setBits]=useState(8);const m=models[index];return <NewsShell index={3} title="MiMo V2.6 — 大きなモデルの、一部を動かす" lead="ProとFlashを切り替え、全体の重みと計算に使う重みの違いを見てみましょう。APIなら自分のGPUなしで試せます。" sources={[["Pro公開ウェイト",models[0].url],["Flash公開ウェイト",models[1].url],["API利用手順","https://mimo.mi.com/docs/en-US/quick-start/summary/first-api-call"],["APIの仕様","https://mimo.mi.com/docs/en-US/api/chat/openai-api"]]}><div className="news-cards">{models.map((model,i)=><button key={model.name} aria-pressed={index===i} onClick={()=>setIndex(i)}><b>MiMo V2.6 {model.name}</b><small>全体 {model.total}B ／ アクティブ {model.active}B</small></button>)}</div><div className="lab-grid"><section className="lab-panel"><h2>MoE：必要な専門家を選ぶ</h2><div className="news-grid" aria-label="専門家の選択を示す概念図。実際の内部動作の計測ではありません。">{Array.from({length:m.experts},(_,i)=><i key={i} className={i<m.selected?'on':''}/>)}</div><p>ルーティング対象の専門家は{m.experts}個、そのうち8個を選択する構造です。図の色は仕組みの説明で、入力に対する実際の選択結果ではありません。</p><div className="lab-metrics"><Metric label="全体のパラメータ" value={m.total+'B'}/><Metric label="アクティブ" value={m.active+'B'}/><Metric label="コンテキスト" value="1M"/></div><p>アクティブな部分が小さくても、選ばれうる重み全体を保持する必要があります。「{m.active}Bだけ用意すればよい」という意味ではありません。</p></section><section className="lab-panel"><h2>公開ウェイトを自分で動かすなら</h2><label className="lab-label">1パラメータの仮定<select value={bits} onChange={e=>setBits(Number(e.target.value))}><option value={16}>16 bit</option><option value={8}>8 bit</option><option value={4}>4 bit（仮の量子化計算）</option></select></label><div className="news-stat">約 {(m.total*bits/8).toLocaleString()} GB</div><p>全パラメータ数×bit数÷8の単純計算（10進GB）。実際はKVキャッシュ・実行用メモリ・量子化の付加情報が必要です。最低VRAMや動作保証の値ではありません。</p><p>公開モデルのライセンスはMITです。公開ウェイトの利用と、提供元APIの利用規約・料金は別です。</p><a className="lab-inline-link" href={m.url} target="_blank" rel="noopener noreferrer">{m.name}のモデルカード・起動手順 ↗</a></section></div><section className="lab-section"><h2>自分のGPUを使わずにAPIで試す</h2><p>選択中：{m.api}。提供元のAPIを呼び出します。このサイトで公開ウェイトをロードする構成ではありません。API版と公開チェックポイントの完全な同一性は保証しません。</p><p>按量課金用のMiMoキーを使用してください。Token Plan専用キーは別の接続先のため、このフォームでは使えません。<a className="lab-inline-link" href="https://platform.xiaomimimo.com" target="_blank" rel="noopener noreferrer">キー・残高を確認 ↗</a></p></section><section className="release-section"><TextTrial provider="mimo" config={{model:m.api}} keyLabel="MiMo APIキー（按量課金用）" description="依頼文をXiaomi MiMo APIへ送信します。この画面はテキスト生成のみです。" initialPrompt="次の作業を、失敗したときに安全に再開できる3段階の計画にしてください。CSVから注文を読み込み、在庫を確認し、発送候補一覧を作る。二重処理と在庫不足への対策も示してください。"/></section></NewsShell>}
