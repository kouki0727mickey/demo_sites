let ready;
self.onmessage=async({data})=>{try{if(!ready){importScripts('https://cdn.jsdelivr.net/pyodide/v0.27.7/full/pyodide.js');ready=loadPyodide({indexURL:'https://cdn.jsdelivr.net/pyodide/v0.27.7/full/'});}const py=await ready;const response=await fetch('/demos/python/quote.py');if(!response.ok)throw Error('Pythonコードを読み込めません');await py.runPythonAsync(await response.text());py.globals.set('payload',JSON.stringify(data));const result=await py.runPythonAsync('json.dumps(quote(json.loads(payload)))');postMessage({result:JSON.parse(result)});}catch(e){postMessage({error:String(e.message||e)});}};
